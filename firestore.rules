rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Mesmo princípio do firestore.rules: só quem passou pelo app (autenticação
    // anônima) pode enviar/ler fotos.
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
